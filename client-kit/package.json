{
  "name": "chatflow-client-kit",
  "version": "1.0.0",
  "description": "ForgeAI reusable single-client chatbot — deploys to Render free tier in <15 min",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.2.0",
    "openai": "^4.104.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
